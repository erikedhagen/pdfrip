[build]
builder = "nixpacks"

[build.nixpacks]
providers = ["python"]
installCmd = "pip install uv && uv sync --no-dev --frozen"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
